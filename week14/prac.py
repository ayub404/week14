# numbers = [5, 2, 9, 1, 7]
# numbers.sort(key=lambda x: -x)
# print(numbers)

# names = ["Alice", "Bob", "Charlie"]
# scores = [90, 85, 95]
# result = list(zip(names, scores))
# result.sort(key=lambda x: (x[1], x[0]))
# print(result)

# numbers = [1,2,3,4,5]
# square = map(lambda x: x*x, numbers)
# print(list(square))

# text = ["apple", "Banana", "CHERRY"]
# sortedd = list(map(lambda x: x.lower(), text))
# print(sortedd)

# letters = ["a", "b", "c", "d"]
# for num, letter in enumerate(letters, start = 1):
#     print( f"{num}. {letter}")

# numbers = [1,2,3,4,5,6]
# evens = list((lambda x: x % 2 == 0, numbers))
# print(evens)

# words = ["apple", "banana", "cherry", "date"]
# sortedd = list(filter(lambda x: len(x) > 5, words))
# print(sortedd)

# A = {1,2,3,4}
# B = {3,4,5,6}

# # Union
# print("Union:", A | B)

# # Intersection
# print("Intersection:", A & B)

# # Difference
# print("Difference A-B:", A - B)

# # Symmetric Difference
# print("Symmetric Difference:", A ^ B)






url = "https://parallelum.com.br/fipe/api/v1"